function perimeter() {
  arguments; // все аргументы функции

  console.log("arguments ", arguments);
}

perimeter(1, 4, 5, 6, 7, 2, 2);

// по правилам хорошего кода у функции должно быть не больше пяти аргументов